# algorithm

These algorithms compare elements to determine their order.

    Bubble Sort
    Selection Sort
    Insertion Sort
    Merge Sort
    Quick Sort
    Heap Sort
    Shell Sort
    Tim Sort (used in Python's sorted() and Java's Arrays.sort())
    Introsort (hybrid of QuickSort, HeapSort, and InsertionSort)
    Tree Sort (using Binary Search Trees)
